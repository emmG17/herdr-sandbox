[runtime]
image = "localhost/herdr-codex-worker:latest"
network = "online"

[resources]
cpus = 4
memory = "8g"
pids = 1024

[codex]
home = "codex"

# Configure the fixed inputs used by the noninteractive Execute Task action.
# [execute]
# id = "WORKER-001"
# prompt = "Implement the requested change and run the relevant tests."
